╔══════════════════════════════════════════════════════════════════════════════╗
║                SERVER UPDATE INSTRUCTIONS v6.7.29                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

📅 ДАТА: 2025-11-03
📦 ВЕРСИЯ: v6.7.29 - Enhanced Referral System
🔗 РЕПОЗИТОРИЙ: https://github.com/alexshangin96-ux/quantum-nexus

═══════════════════════════════════════════════════════════════════════════════
                    НА СЕРВЕРЕ ВЫПОЛНИТЕ ЭТИ КОМАНДЫ:
═══════════════════════════════════════════════════════════════════════════════

cd /root/quantum-nexus

# Удаляем конфликтующие backup файлы
rm -f web_app.html.backup web_server.py.backup

# Пулим изменения из GitHub
git pull origin main

# Бэкапим БД (на всякий случай)
sudo -u postgres pg_dump quantum_nexus > /root/quantum-nexus-backup-$(date +%Y%m%d-%H%M%S).sql

# Применяем миграцию БД
sudo -u postgres psql quantum_nexus -f ADD_IS_PREMIUM.sql

# Копируем обновлённые файлы
sudo cp web_app.html /var/www/quantum-nexus/
sudo cp web_server.py /var/www/quantum-nexus/
sudo cp handlers.py /var/www/quantum-nexus/
sudo cp models.py /var/www/quantum-nexus/
sudo cp config.py /var/www/quantum-nexus/

# Перезапускаем сервисы
sudo systemctl restart quantum-nexus-web.service
sudo systemctl restart quantum-nexus.service

# Проверяем статус
sudo systemctl status quantum-nexus-web.service --no-pager
sudo systemctl status quantum-nexus.service --no-pager

═══════════════════════════════════════════════════════════════════════════════
                    ИЛИ ИСПОЛЬЗУЙТЕ СКРИПТ:
═══════════════════════════════════════════════════════════════════════════════

cd /root/quantum-nexus
bash UPDATE_SERVER_FIXED.txt

═══════════════════════════════════════════════════════════════════════════════

✅ После обновления проверьте:
   • Реферальная система открывается
   • Статистика Premium/Regular отображается
   • Toast-уведомления работают
   • Бонусы начисляются корректно

═══════════════════════════════════════════════════════════════════════════════


